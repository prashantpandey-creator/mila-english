// Kept as a compatibility component while page headers migrate away from the
// old per-page palette control. Mila now has one intentional surface system;
// immersive voice routes choose their neutral focus surface automatically.
export default function ThemeToggle() {
  return null;
}
