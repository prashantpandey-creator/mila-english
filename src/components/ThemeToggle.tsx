// Kept as a compatibility component while page headers migrate away from the
// old per-page palette control. Mila now has one intentional light surface.
export default function ThemeToggle() {
  return null;
}
