/**
 * Gia launches free-first. Keep this product decision explicit and shared by
 * the client and session boundary so a UI change cannot drift from entitlement.
 * Pro can be introduced later without making the current free door deceptive.
 */
export const GIA_LIVE_FREE_LAUNCH = true;
